{
  "name": "custom-seas-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bot Discord Custom Seas Lounge — inscription, profil, ladder (réutilise la couche du site).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "deploy": "tsx src/deploy-commands.ts",
    "setup-server": "tsx src/setup-server.ts"
  },
  "dependencies": {
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5"
  }
}
